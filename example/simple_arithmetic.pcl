; two pass label; output 30

_start:
	jmp compute

output:
	print r0
	halt

compute:
	push 10         ; 10 to stack
	push 20         ; 20 to stack: [10, 20]

	pop r0          ; r0 = 20
	pop r1          ; r1 = 10
	add r0, r1

	jmp output
